import {StaticTranslationLoader} from './static-translation-loader';

describe('StaticTranslationsLoader', () => {
  it('should create an instance', () => {
    expect(new StaticTranslationLoader()).toBeTruthy();
  });
});
