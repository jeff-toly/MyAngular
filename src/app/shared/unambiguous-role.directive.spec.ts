import { UnambiguousRoleDirective } from './unambiguous-role.directive';

describe('UnambiguousRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new UnambiguousRoleDirective();
    expect(directive).toBeTruthy();
  });
});
